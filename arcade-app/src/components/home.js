import "./styling.css";

export default function Home(){
    return (
        <div>
            <header>
                <p>This is the automatic landing page.</p>
            </header>    
        </div>
    );
}